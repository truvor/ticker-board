import { Routes } from '@angular/router';
import { TickerBoard } from './components/ticker-board-component/ticker-board-component';

export const routes: Routes = [
    { path: '', component: TickerBoard }
];
